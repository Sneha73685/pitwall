"""Two-lap comparison domain logic (M6): distance-grid alignment, delta
computation, sector aggregation, and comparability validation.

Empty in Phase 1 -- alignment.py, delta.py, sectors.py, and validation.py
are added in Phase 2. Pure functions only, no FastAPI/route imports, per
the existing app/services/ boundary (empty since M0, reserved for exactly
this: "populated as real endpoints arrive").
"""
