"""TelemetryProvider interface and implementations (e.g. FastF1Provider).

See docs/adr/0005-telemetry-provider-abstraction.md. Intentionally empty
in M0 -- the interface is designed around the normalized internal
schema, which is itself M1 design work (docs/data-model.md, TBD).
"""
